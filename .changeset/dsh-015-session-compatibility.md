---
"dsh-mnemon": patch
"dsh-mnemon-source-runtime": patch
"dsh-mnemon-source-documents": patch
"dsh-mnemon-source-memory-spaces": patch
---

Support the published DSH 0.1.5-rc.1 cohort. Restore Memory System RPC routes through the Starter connection dependency patch, emit valid recall/instruction metadata, and provide an explicit copy-only repair command for affected legacy v0 Session logs. Retain earlier DSH peer ranges and align Source Client development dependencies with the published UI primitives.

兼容已发布的 DSH 0.1.5-rc.1 依赖族。通过 Starter 的连接依赖补丁恢复记忆系统 RPC 路由，写入合法的 recall/instructions 元数据，并提供仅生成副本的显式命令，修复受影响的旧 v0 会话日志。保留旧版 DSH peer 范围，同步 Source Client 开发依赖与已发布 UI primitives。
