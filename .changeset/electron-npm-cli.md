---
"dsh-mnemon": patch
"dsh-mnemon-source-memory-spaces": patch
---

Run verified npm JavaScript launchers in Node mode when DSH is embedded in Electron. Fix Mnemon memory operations, CLI version detection, npm ownership checks, and updates while preserving embedding overrides and direct native executable behavior.
